//A C++ program to enter any number and calculate its square root

#include <iostream>
#include <cmath>
using namespace std;

int main() {
	double param;
	cout << "Enter a number:" << endl;
	cin >> param;
	cout << "\nSquare root of " << param << " = " << sqrt(param);
}
