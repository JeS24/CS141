//Write a C++ program to enter two numbers and find their sum.

#include <iostream>
using namespace std;

int main() {
	double a, b;
	cout << "Enter two numbers: " << endl;
	cin >> a >> b;
	cout << "\nSum of " << a << " and " << b << ": " << (a+b);
	return 0;
}
